/**
 * Created by aschey on 9/24/16.
 */
package DPL;

public enum TokenType {
    O_BRACKET,
    C_BRACKET,
    COMMA,
    SEMICOLON,
    PLUS,
    MINUS,
    COLON,
    ASSIGN,
    STAR,
    SLASH,
    LT,
    GT,
    LEQ,
    GEQ,
    EQ,
    NEQ,
    VAR,
    DEF,
    VARIABLE,
    INTEGER,
    STRING,
    IF,
    ELSE,
    FOR,
    WHILE,
    UNKNOWN,
    NONE,
    END_OF_INPUT
}
