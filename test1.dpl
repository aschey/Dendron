`test
def f[a b]
[
    while [a < 5]
    [
        print['hi'];
        ` random
    ]

    for [i 5 10 2]
    [
        println['hi'];
    ]

    if [a == 2]
    [
        f[a f[a b]];
        f[a];
        `f[lambda[a b] [ return[a]; ]];
    ]
    else if [a == 3]
    [
    ]

    `var b = [a b];
    var b = f[[f[4-5]]];
    var c = 2 + 3 * 7 / 2;
    `*b[0];
    `*b[0] = 2;

    `var c = ['a':1 4:2];
    `*c[4];
]



`this is a comment

`unary: integer, string, ID, open paren expr close paren, functionCall

`varExpir: ID, ID openparen, optarglist, closeparen
